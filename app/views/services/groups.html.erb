<%= label_tag :group_id, t(:group_name), :class => "inline" %>
<%= collection_select(:group, :id, @groups, :id, :name, 
                     {:include_blank => t(:all_groups), :selected => @selected_group }) %>