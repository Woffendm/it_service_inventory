<div class="container-fluid filter">
  
  <span class="float_right">
    <%= button_tag l(:label_btn_submit_selected), :class => "btn btn-primary btn_submit", 
        :id => "btn_filter_issues_onlist" %>
    
    <% if @use_version_for_sprint %>
      <%= select_tag("versions", options_from_collection_for_select(@sprints, :id, 
          :name, :selected => session[:params_version_ids]), {:prompt => "All Sprints"}) %>
    <% else %>
      <%= select_tag("custom_values", options_for_select(@sprints, 
          :selected => session[:params_custom_values]), {:prompt => "All Sprints"}) %>
    <% end %>
    
    <%= select_tag("projects", options_from_collection_for_select(@projects, :id, :name,
        :selected => session[:params_project_ids]), :prompt => "All Projects") %>
    
    <%= select_tag("members", options_from_collection_for_select(@members, :id, :name,
        :selected => session[:params_member_ids]), :prompt => "All Members") %>
    
    <%= select_tag "statuses", options_from_collection_for_select(@issue_statuses, 
        :id, :name), :selected => session[:params_status_ids], :prompt => "All Statuses" %>
  </span>
  
</div>