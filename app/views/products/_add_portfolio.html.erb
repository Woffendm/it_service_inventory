<% unless possible_portfolios.blank? %>

  
  <div class="modal hide fade" id="add-portfolio">
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
      <h4> <%= t(:add) + t(:portfolio) %> </h4>
    </div>
    <%= fields_for "new_product_portfolio" do |form|%>
      <div class="modal-body form-inline">
        <%= form.label :portfolio_id, t(:portfolio) %>
        <%= form.select(:portfolio_id, options_from_collection_for_select(
                        possible_portfolios, :id, :name),
                        {:include_blank => t(:select) + t(:portfolio)}, 
                        {:class => "autocomplete large-select"}) %> 
        <%= form.hidden_field(:product_id, :value => product.id) %>
      </div>
      <div class="modal-footer">
        <a class="btn" data-dismiss="modal" aria-hidden="true"> <%= t(:cancel) %> </a>
        <%= form.submit t(:add), :class => "btn btn-success" %>
      </div>
    <% end %>
  </div>
  
  
<% end %>